type Cap = `${number}cap`;
type Ch = `${number}ch`;
type Em = `${number}em`;
type Ex = `${number}ex`;
type Ic = `${number}ic`;
type Lh = `${number}lh`;
type Rem = `${number}rem`;
type Rlh = `${number}rlh`;
type Vh = `${number}vh`;
type Vw = `${number}vw`;
type Vmin = `${number}vmin`;
type Vb = `${number}vb`;
type Vi = `${number}Vi`;
type Px = `${number}px`;
type Cm = `${number}cm`;
type Mm = `${number}mm`;
type Q = `${number}Q`;
type In = `${number}in`;
type Ppc = `${number}pc`;
type Pt = `${number}pt`;

type Length =
	| Cap
	| Ch
	| Em
	| Ex
	| Ic
	| Lh
	| Rem
	| Rlh
	| Vh
	| Vw
	| Vmin
	| Vb
	| Vi
	| Px
	| Cm
	| Mm
	| Q
	| In
	| Ppc
	| Pt;

export default Length;
